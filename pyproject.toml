[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "quickcfg"
version = "1.0.0"
description = "A simple way to change a line in a file."

[project.scripts]
quickcfg = "quickcfg:main"